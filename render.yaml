services:
  - name: fastapi-service
    type: web
    env: python
    buildCommand: |
      pip install -r requirements.txt
      apt-get update && apt-get install -y nginx
    startCommand: |
      service nginx start && uvicorn main:app --host 0.0.0.0 --port 8000
